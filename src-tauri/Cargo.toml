[package]
name = "ai-task-manager"
version = "0.2.0"
description = "AI Task Manager — a faster, smarter Windows Task Manager."
authors = ["Razee4315"]
edition = "2021"
rust-version = "1.77"

[lib]
name = "ai_task_manager_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
tauri-plugin-shell = "2"
tauri-plugin-dialog = "2"
tauri-plugin-store = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.33"
parking_lot = "0.12"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync"] }
once_cell = "1"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
anyhow = "1"
thiserror = "2"
sha2 = "0.10"
hex = "0.4"
chrono = { version = "0.4", default-features = false, features = ["clock", "serde"] }
# Feature 3: Historical Metrics Storage
rusqlite = { version = "0.31", features = ["bundled"] }
# Feature 4: App data path resolution
dirs-next = "2"

[target.'cfg(windows)'.dependencies]
windows-service = "0.7"
winreg = "0.55"
windows = { version = "0.58", features = [
  "Win32_Foundation",
  "Win32_System_Services",
  "Win32_UI_WindowsAndMessaging",
  # Feature 2: Network Connections — IP Helper API
  "Win32_NetworkManagement_IpHelper",
  "Win32_Networking_WinSock",
] }

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "s"
strip = true
incremental = false
